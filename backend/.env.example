# PaperTrail Backend Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# Server Configuration
NODE_ENV=production
PORT=3000

# Frontend Origin (CORS allowlist)
# Set this to your Netlify frontend URL in production
# Example: https://papertrai1.netlify.app
FRONTEND_ORIGIN=https://papertrai1.netlify.app

# JWT Secret (REQUIRED)
# Generate a strong random secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# This MUST be set and kept secret - used to sign JWT tokens
JWT_SECRET=your-secret-key-here-minimum-32-characters-recommended-64

# Database Configuration (REQUIRED)
# MySQL database connection details
DB_HOST=your-database-host
DB_PORT=3306
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_NAME=your-database-name

# Security Notes:
# - JWT_SECRET: Use a strong, random value. Rotate periodically.
# - DB_PASSWORD: Use a strong password for database access.
# - FRONTEND_ORIGIN: Must match your frontend deployment URL exactly.
# - NODE_ENV: Set to 'production' in production environment.
